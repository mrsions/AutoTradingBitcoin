pip freeze > .\installed.txt ; pip uninstall -y -r .\installed.txt; pip cache purge

pip install -r .\requirements.txt



pip freeze > .\installed.txt ; pip uninstall -y -r .\installed.txt; pip cache purge ; pip install -r .\requirements.txt
